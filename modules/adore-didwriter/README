/**
*Adore DID Writer / Ingestion Processor
*/

----------------------
Requirements
----------------------
- Java 1.5

----------------------
Installation
----------------------
1. Copy adore-didwriter to a location of your choosing

2. Configure the following in module.conf:

* module.lib - Define path to lib directory containing project jars (e.g.  lib)
* module.src - Define path to src directory containing project source files (e.g.  src)
* install.home - Define path to root adore installation (e.g. /usr/local/adore)

3. Run "ant -f build-standalone.xml build" to build project
